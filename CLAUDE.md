@AGENTS.md

## Claude Code

- Use plan mode before changing anything under `src/auth/` or `src/storage/path-policy.ts`.
- Run `npm test` before reporting a task done; if `rg` is missing locally, say so — 7 tests skip.
